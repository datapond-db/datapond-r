% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe.R
\name{dp_describe}
\alias{dp_describe}
\title{Explore a database's data dictionary}
\usage{
dp_describe(x, table = NULL, search = NULL)
}
\arguments{
\item{x}{A database id, or an open connection from \code{\link[=dp_connect]{dp_connect()}}.}

\item{table}{Describe the columns of this table instead of listing tables.}

\item{search}{Find columns whose name contains this text (case-insensitive).}
}
\value{
A data frame (a tibble when the tibble package is installed).
}
\description{
Every datapond database ships \verb{_metadata} (one row per table) and
\verb{_columns} (types, null rates, example values, join hints). \code{dp_describe()}
reads them and falls back to \code{information_schema} when they are absent.
}
\examples{
\dontrun{
dp_describe("eoir")
dp_describe("eoir", table = "proceedings")
dp_describe("eoir", search = "judge")
}
}
