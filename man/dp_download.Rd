% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{dp_download}
\alias{dp_download}
\title{Download a database for local use}
\usage{
dp_download(id, path = NULL, quiet = FALSE, resume = TRUE)
}
\arguments{
\item{id}{Database id.}

\item{path}{Destination file or directory. Defaults to \code{\link[=dp_data_dir]{dp_data_dir()}}.}

\item{quiet}{Suppress the progress bar?}

\item{resume}{Resume a partial download if one exists?}
}
\value{
The local path, invisibly.
}
\description{
Downloads the full \code{.duckdb} file so later queries run at disk speed.
Files are saved as \verb{<id>.duckdb} under \code{\link[=dp_data_dir]{dp_data_dir()}} (or \code{path}), which is
where \code{dp_connect(id, local = TRUE)} looks for them.
}
\examples{
\dontrun{
dp_download("dol-visas")
con <- dp_connect("dol-visas", local = TRUE)
}
}
