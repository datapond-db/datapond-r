% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{dp_download}
\alias{dp_download}
\title{Download a database for local use}
\usage{
dp_download(id, path = NULL, quiet = FALSE, resume = TRUE)
}
\arguments{
\item{id}{Database id.}

\item{path}{Destination file or directory. Defaults to \code{\link[=dp_data_dir]{dp_data_dir()}}.}

\item{quiet}{Suppress the progress bar?}

\item{resume}{Resume a partial download if one exists?}
}
\value{
The local path, invisibly.
}
\description{
Downloads the full \code{.duckdb} file so later queries run at disk speed.
Files are saved as \verb{<id>.duckdb} under \code{\link[=dp_data_dir]{dp_data_dir()}} (or \code{path}), which is
where \code{dp_connect(id, local = TRUE)} looks for them.
}
\details{
The transfer goes to \verb{<file>.part} and replaces the destination only after
it is complete and opens as a DuckDB database, so an existing copy is never
damaged by a failed download. A partial download is resumed only when the
server identifies the file's revision (ETag, else Last-Modified), the partial
file was started against that revision, and the ranged request is bound to it
with \code{If-Range}; otherwise it restarts. A sidecar \verb{<file>.datapond.json}
records the remote file identity for \code{\link[=dp_update]{dp_update()}}.
}
\examples{
\dontrun{
dp_download("dol-visas")
con <- dp_connect("dol-visas", local = TRUE)
}
}
