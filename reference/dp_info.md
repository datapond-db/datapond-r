# Information about one database

Information about one database

## Usage

``` r
dp_info(id)
```

## Arguments

- id:

  Database id.

## Value

The registry entry (a list of class `datapond_db`), printed in a
readable layout.

## Examples

``` r
if (FALSE) { # \dontrun{
dp_info("eoir")
} # }
```
